export default class Field {
    constructor(id, label, placeholder, options, type) {
        this.id = id;
        this.label = label;
        this.placeholder = placeholder;
        this.options = options;
        this.type = type;
    }
}